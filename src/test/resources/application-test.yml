spring:
  config:
    activate:
      on-profile : testDB
  datasource:
    driver-class-name : com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/selfmadeblogtest
    username : root
    password: 111111